# SpiritPet TODO

## 当前阶段：MVP 收尾

**方向**：美术资源开发**暂时搁置**，优先把体验与核心流程做好，美术留到核心体验稳定后再集中处理。

**MVP 范围**（设计文档 §15.1）：只验证「蛋展示 → AI 对话 → 亲密度 → 破壳」的完整闭环。
**明确不做**（留到 v1.0）：多分支进化、系统托盘、长期记忆。

### 已完成的 MVP 闭环

- [x] §3.2 蛋的可视化反馈：每 +5 亲密度多一道裂纹（共 6 道）、裂缝透光、蛋壳由灰转暖黄、晃动随亲密度加快
- [x] §3.3 破壳方式随机（三套裂纹走向）
- [x] §3.3 首次 MBTI 分配改为**分析破壳前的对话风格** + 10~20% 随机偏移（不再是掷骰子）
- [x] 破壳四阶段仪式：摇晃 → 裂纹逐条裂开 → 白光爆裂碎壳飞散 → 宠物诞生 + 性格揭晓
- [x] §4.2 对话加分（蛋期 +2 / 宠物 +1，每日上限 20 轮）+ 早安/晚安 +3（每日各 1 次）
- [x] §4.4 跨天结算衰减 0.5%/天，按实际间隔天数补齐
- [x] 16 种 MBTI 人格提示词（说话节奏 / 标点习惯 / 关心方式 / 绝对不做的事 / 示例对话）
- [x] 宠物自主行为系统（跳跃 / 摇摆 / 果冻 / 张望 / 打瞌睡）
- [x] 右键菜单「🔄 重新养一只」，便于反复体验破壳

### 待验证

- [ ] 用反差极大的两种说话风格各养一只，确认「性格因人而成」真的成立
- [ ] 亲密度节奏体感：12~15 轮破壳是否合适

### 已就绪但搁置

美术管线（见 `docs/ART.md`）已经打通：`npm run gen:pets` 可批量生成 16 种性格的插画，
`Pet.vue` 已支持插画渲染 + 缺图自动回退 SVG。核心体验稳定后随时可以捡起来。

---

## 待启用功能

### Jev 决策路由（代码已集成，等待 API Key）

**状态**：代码已完成，设置面板已就位，但 TypeSafe AI 当前注册不了，暂不启用。

**启用方式**：在设置面板填 `Jev API Key` + `Jev Base URL`（默认 `https://api.typesafe.ai`），保存即生效，**无需改代码**。

**Jev 是什么**：TypeSafe AI 的 System One 决策模型，不生成文字，只做结构化判断（Choice / Score / Noul）。
- 价格：输入 $0.042/百万 token，**输出免费**
- 注册赠 $5 额度（够用几十万次调用）
- 文档：https://docs.typesafe.ai/

**为什么要它**：省 token 的关键不是"本地硬编回复"，而是**减少上下文长度**。
简单对话（"早安"）不需要 20 条聊天历史，只带最近 2 条就够，回复仍由 LLM 生成 —— 保持新鲜感的同时省掉 90% 上下文 token。

**路由逻辑**（见 `src/stores/petStore.ts` 的 `callJevRouter` / `chat`）：

```
用户说话
  ↓
有 Jev Key？
  ├─ 没有 → 走默认逻辑（20 条完整上下文）
  └─ 有   → Jev 分类消息类型
            ├─ greeting / casual → contextMsgCount = 2（短上下文）
            └─ emotional / query / command → contextMsgCount = 20（完整）
  ↓
调 LLM 生成回复
```

**已埋的坑（已修）**：Base URL 已含 `/v1` 时不能再追加 `/v1`，否则拼成 `/v1/v1/chat/completions` 报 `Failed to fetch`。

---

## 后续版本规划

### v1.0
- 系统托盘 + 右键菜单
- 多分支进化（MBTI 维度演变）
- 长期记忆（NDJSON + sqlite-vec）
- TTS 语音互动实验

### v1.1
- 记忆回声（1-2 天前的对话回顾）
- 语音互动生产化

### 架构备注
- Go 后端服务（用于常驻进程单二进制交付）：MVP 未使用，必要时引入